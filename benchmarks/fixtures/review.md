# Fixture

A merge authorization record is searched by operation, but execution never marks the matching grant consumed. A second merge therefore passes. Classify the defect, explain user impact, and state the required behavior.
