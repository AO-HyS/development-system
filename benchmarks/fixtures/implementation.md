# Fixture

`authorizeImplementPreview(state, terminalSlice)` currently accepts an empty `terminalSlice` and records delivery authority. Return a minimal patch that rejects the transition as `denied` without side effects, plus the public acceptance test.
