# 1.26.0 operational recovery validation

Validation completed on 2026-09-21. Source, installation and actual-host evidence
are recorded separately; copying installed files alone does not certify enforcement.

The retained run `jev1260-t3-proof-d-20260921` reached real Computer Use and
displayed Pending, but its returned JPEG bytes were declared PNG by the host.
Strict declared-MIME validation rejected persistence and the former catch hid
the reason. The repair detects supported signatures, retains differing declared
MIME and original output hashes, and exposes safe capture diagnostics.
Administrative recovery closes only a proven completed failed host attempt;
the original run remains blocked. A fresh successor must establish negative and
positive actual-image assessment under the repaired runtime.

The successor `jev1260-t3-proof-f-20260921` persisted the actual host image as
JPEG while retaining its declared PNG MIME and unchanged byte hash. Its
independent plan and final reviews passed. The image assessor then exposed a
separate launcher defect: Codex's variadic `--image` option consumed the prompt,
so the process exited before observing a provider session. That run remains
blocked with its capture and reviews preserved. The launcher repair separates
the positional prompt with `--`; startup diagnostics alone are not acceptance.

The functional review required mixed check/visual verification, actual host
tool shape discovery, precise missing-field errors, Linear document/update
adapters, and runtime-byte freshness. These corrections have dedicated isolated
regressions. Existing ownership, replay, interruption and acceptance assertions
remain enabled. A prior security review was interrupted by automatic review and
has not been retried or counted as a pass.

Operational scope: known exact Codex shell/patch and Linear tools, Computer Use
outer calls, attached model/check process launch and observed completion. Inner
Computer Use statements and external-provider internal tools are not separately
enforced. Unknown tools and unproven native writable children remain explicit
capability gaps. No silent provider fallback is introduced.

The final runtime candidate is `2e7b909f5f73eab0c030a06628131fef97d316f2`.
Independent source and exact-test reviews passed without findings. `pnpm run verify`
passed 753/753 tests, type checking, roster checks and repository validation.
`pnpm run scenario` passed all 28 scenario tests plus isolated-HOME installation,
drift detection, failed validation, reinstall, rollback and unrelated-file preservation.
The generated 60 artifacts and 13 runtime files passed release consistency checks.
The release archive is built from the committed canonical tag and carries its own
source-commit and file-hash inventory; GitHub publishes the exact built archive.

Actual run `jev1260-t3-proof-g-20260921` closed **accepted**. The installed runtime
hash was `01b3663e6808252abf451d6aa125532814868253c9604162d9e58b3e8a68efd4`.
The fresh observed Astra XHigh plan and final reviews both passed. Two distinct
fresh observed Astra XHigh assessors inspected the actual runtime-bound images:

- Pending: C1 **fail**, assessment `assessment_98bbdb92-96ef-4346-a96e-e09d3d902ec4`;
  image SHA-256 `d6f9f65675cc7b0b60ab9a2709d24b572ac7d6895a897c1dd5c5c1b6959b3b43`.
- Verified after activating Verify: C1 **pass**, assessment
  `assessment_0be9b039-cd93-4567-9ead-9abe57ff3131`; image SHA-256
  `19bc501e43d829b543a61a3d5de5f7741662d72af1467f98df06c3a42d357e77`.

Both captures retained their exact JPEG bytes and the host's differing declared
PNG MIME. All processes terminated, all attempts completed, no leases or findings
remained. The original run d remains blocked with its owner, four completed role
attempts, plan and two reviews preserved; supported recovery resolved only its
terminal failed capture. No historical evidence was imported as current acceptance.

NutriPlan was updated first through the supported installer and repository
normalizer. Its managed repository contract and Codex adapter use 1.26.0. All
554 unrelated baseline paths retained their hashes; no product source was changed.

The historical NutriPlan intake was recovered using the installed command from
an actual fresh Astra XHigh session at the NutriPlan root. The recorded recovery
session is `01a0c55c-1b12-7550-a7c3-458399246cde`; the original owner remains
`01a0c50a-6662-7a72-8c7d-9592e823654d`. The old run is closed/blocked with zero
attempts and leases, retaining its history. This releases obsolete intake state;
it does not claim acceptance or implementation of NUTRI-346. No product file was
changed by recovery.
