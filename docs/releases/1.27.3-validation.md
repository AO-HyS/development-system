# Local maintenance validation: 1.27.3

Date: 2026-09-22. Baseline: official 1.27.1 commit
`bc91359ac20c9a5744c4824950acd27b2b16f8d5`.

This candidate adds the explicitly selected Astra XHigh writer/researcher route.
It does not change the default provider recommendation, publish a release or
certify native writable descendants. Maintenance follows ADR 0048.

## Source and focused evidence

- Independent plan review required command validation before spawn and an actual
  sandbox probe; both were implemented.
- Independent security review found inherited automatic approvals and managed
  permission-profile selection could defeat legacy sandbox flags. The corrections
  pin never approvals, preflight typed effective managed requirements before writer
  binding, and verify actual permission metadata on return. The same reviewer
  closed both findings with no additional actionable security defects.
- `pnpm typecheck`: passed.
- `node --test test/governance-codex-permissions.test.mjs
  test/governance-astra-writer.test.mjs test/governance-integration-root.test.mjs`:
  72 passed, zero failed/skipped.
- Focused adapter, recovery-executor, lifecycle and writer-argument checks:
  20 passed, zero failed/skipped.
- Package distribution and governance installation checks: 9 passed, zero failed.
- Test-change policy: no violations. The parent reviewed the exact additive
  test drafts before applying their hash-bound exceptions.
- `build-release-1273.mjs --write` then `--check`: 61 artifacts and all 14 runtime
  modules covered. Existing catalog 0.46.1 and published artifacts remain intact.
- `git diff --check`: passed. Full repository verify/scenario was not run.

Synthetic provider fixtures prove actual process execution, termination, command
binding and file reconciliation, not a live provider identity. The new integration
cases separately demonstrate accepted owned writes, rejected out-of-scope writes,
unconfirmed/mismatched identity rejection, preserved dirty host files and explicit
subsequent integration. Malformed commands cannot persist a binding or spawn.

## Current host observations

An actual Codex 0.156.0 Astra XHigh session ran the isolated sandbox probe with
an expanded default permission selection and automatic-review configuration, plus
the restrictive writer overrides. It wrote the owned fixture; sibling, parent,
temporary-directory, `/tmp` and loopback network attempts returned `EPERM`.
Actual turn metadata showed never approvals, the expected workspace-write policy,
restricted networking and no writable root outside the candidate.

The actual app-server requirements preflight completed with its process terminated
before any writer was started. Only a requirements hash and process receipt were
retained; no raw configuration or credentials were printed. Private operational
receipts are kept outside the public repository.

The previous real-HOME installation audited healthy at 1.27.1. Packed candidate
upgrade, reinstall, rollback and real-HOME installation are subsequent operations;
their receipts must identify the exact committed source and archive. A completed
governed Astra writer receipt remains required before claiming operational support
for the repaired route. Preflight cannot make later administrator-policy changes
atomic with the writer process.
