# 1.27.0 separate host and integration worktrees

The observed conversation directory can now be declared separately from the
integration worktree. The approved design preserves authentic host identity,
candidate Git identity and HEAD, third-workspace writers, path confinement,
root ownership, termination evidence and current acceptance.

Independent plan and implementation reviews approved the candidate, including
explicit inspection cwd, legacy writer reservations, versioned run/registry
envelopes and termination-before-freshness reconciliation. Review findings about
administrative recovery and actual process termination were corrected and
reviewed again before the final checks.

Focused verification passed:

- 50 integration-root tests, including real divergent Git worktrees, processes,
  recovery, hook ingress, rejected paths and ownership, and raw exit evidence.
- 84 existing affected lifecycle, recovery, adapter, executor and CLI tests.
- 34 release, installation, current-version and distribution tests.
- TypeScript validation and reviewed exact-hash test-change policy.
- Isolated 1.26.1 to 1.27.0 installation, unchanged hook bytes, drift rejection,
  reinstall, installation-only reversal and preservation of unrelated files.

The 50-test file has SHA-256
`7e6b4d3042998d7c34ab1e882e0b53a8841e09745932645046e53d7c7d79af5a`.
Provider/Jev identities in these tests are synthetic and do not certify
operational host behavior. Publication and actual installation are separate
steps; current observed host execution remains to be verified after installation.

Run and registry envelopes use schema version 2. Once a version-2 registry has
been written, reinstalling 1.26.1 is not an operational rollback: its older
reader rejects that registry. Forward repair or a separately reviewed migration
is required. The installation reversal above explicitly verifies that no runtime
registry exists; it does not assert downgrade compatibility for active state.

The user authorized maintenance and a new immutable release to unblock NutriPlan.
Actual installation and current observed host execution remain distinct from
source tests and package publication. Product delivery permissions are retained
independently of this Development System authorization.
