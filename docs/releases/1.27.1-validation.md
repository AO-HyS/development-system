# 1.27.1 complete Jev requests and visible plan packets

The classifier wire representation retains each canonical artifact and dependency
once, with typed artifact references and ordered required dependency IDs. Approved
control boundaries remain supported through their authentic boundary and matching
judgment. Missing, ambiguous, contradictory or unbound records stop before
transport; stored artifacts and schema version 2 remain unchanged.

The explicit request limit is 98,304 bytes (96 KiB). Complete selected sources
and cumulative changed context retain independent 65,536-byte limits, and the
criterion limit remains 200. No input is truncated to obtain a passing judgment.
Read-only status now includes ordered plan packets and producer metadata through
explicit allowlists, excluding transcript paths and private process commands.

Focused local verification passed:

- 68 tests across governance runtime Jev, recovery core, recovery CLI and lifecycle
  suites, with zero failures or skips. These cover exact UTF-8 request/source
  boundaries, cumulative changed context, 200/201 criteria, lossless references,
  approved boundary dependencies, stale bindings and status nonmutation.
- TypeScript validation and `git diff --check`.
- Independent source and exact test-content review.
- 46 focused visual-grill, package-source, package-distribution and
  repository-preparation tests, with zero failures or skips. Package installation
  scenarios in these tests use isolated fixtures rather than the live operator.
- The new release builder writes and checks 60 manifest artifacts, 13 runtime
  snapshots and the eight-file copied orchestration skill. Catalog validation
  checks canonical folder hashes and retains 104 skills and 140 variants.

The retained real NutriPlan planning return was reconstructed read-only as a
79,464-byte request containing all three selected sources, eight criteria and
eight plan packets. Closed-run and registry bytes were unchanged. No credential
was read and no provider request was made for that measurement.

Provider identities in the focused tests are simulated. These results establish
local behavior, not actual Jev provider acceptance or installed host enforcement.
Packaging this candidate, isolated upgrade validation, publication and operational
classification remain separate delivery checks. An actual classification using
the installed candidate is required before operational acceptance; rejection
does not authorize another automatic limit increase.

Catalog 0.46.1 preserves 104 skills and 140 variants. Only the copied
coding-orchestration guidance changes its request-limit paragraph; the remaining
103 skills and 138 variants retain their published metadata and source bytes.
All earlier manifests, catalogs and artifacts remain immutable. Product release
authorization remains separate from the authorized Development System repair.
