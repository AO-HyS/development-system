# Current work — Development System 1.30.0 and NutriPlan launch

## Authorized objective — 2026-09-24

Publish/install an additive development release, retain existing roles and
architecture, ensure current GPT-6 models are genuinely used, retain bounded
Jev advisory and enable the conservative Headroom transport. Then create and
actually start a new T3 Code task for the complete current NutriPlan visual
and behavioral spec with Sol High orchestrator and the existing full roles.
Use actual behavior/screenshots and independent reviews, with corrections.
No automated tests authored or run, no result polling, no renewed questions.
Preserve existing tests and CI; product production promotion is not included.

The user explicitly corrected that the examples of roles were not a new roster:
all existing roles stay. Current model mappings already use Sol6/Luna6/Astra6.
New-task selection must be observed, not inferred from those files. Jev stays
advisory-parent-execution, with no controller/per-tool/Stop gates.

## Root, source and ownership

Canonical root /Users/corrortiz/Documents/AO/development-system. Branch
feat/headroom-observed-delivery-1.30.0 from updated local develop at
b36ca655bd53a3bfffbbc3d5c8aafa83af71aa97 (one pre-existing documentation commit
ahead of origin/develop). Old benchmark continuity and all pending edits were
preserved by commit9f7a124 on fix/workspace-readiness-1.29.1; no worktree/clone
was created. That branch retains the complete historical experiment narrative.

Development prerelease1.30.0 was published and installed from621584a, with
catalog0.49.0. Isolated setup and operator contract audit passed; operator
config bytes and all18 agent TOMLs remained unchanged. Advisory/runtime/roster
component remains1.29.0. The isolated skills audit correctly lacks operational
host evidence; it is not a claim of loaded/influenced skills. Credentials remain
in their existing account locations.

Read-only source mappers completed. Astra XHigh authored the plan; a distinct
fresh Astra reviewer approved it with one correction: explicitly set
HEADROOM_BEACON=off and DO_NOT_TRACK=1 because --no-telemetry does not disable
external beacon upload. Jev actual classification succeeded and the parent
recorded general_implementation; private receipts below.

One Sol writer owns runtime/headroom/** and runtime/native-browser/**. One
Luna exact writer owns the1.30 builder/package/new immutable artifacts and
catalog0.49/ADR/README. Parent owns this file, integration, operator activation,
T3 launch and final evidence. No worker may modify other owners' paths.

## Current private evidence and pending acceptance

Private root ~/.development-system/private/releases/1.30.0-20260924/ contains
plan.md, atom.json, run.json, advice.json and decision.json. Browser evidence
is under browser-readiness/: native T3 DOM, 450px scroll and recording proved
real rendered observation. preview_snapshot itself fails with generic
PreviewAutomationExecutionError. No explicit security denial was observed.
Current generic node_repl omits CUA_REPL_ENABLED_SURFACES; new invocation must
use the documented CUA launcher, preserving security protections and other MCPs.
File installation alone does not establish browser acceptance.

NutriPlan canonical root /Users/corrortiz/Documents/AO/nutri-plan remains on
codex/aoh-168-composite-attestation, SHA5617b54a61946613800115c478daab444ddf292c.
Private baseline receipt/snapshot under nutriplan-baseline/ preserves572
changed/untracked files (expanded listing), about10.3MB, with stable status
during capture; tracked/staged patches retained. No product source modified.
Existing dirty state is not a reason to reset or invent another workspace.
Current user's all-at-once instruction authorizes the latest full spec and
references; do not reopen older gallery approval checkpoints.

Nutri scope: summary-with-viewer-experience-unification spec, screen checklist
and route inventory; NUTRI190, module191–198, foundational345–348, closure366.
Eight flows,155 screen tickets and164 route inventory; responsive web only,
not deprecated apps/mobile. The new coordinator must reconcile exact current
local/Linear scope and dependencies, preserve unrelated baseline changes, and
use real authorized role fixtures without exposing credentials.

Implementation and correction are complete. Source syntax, strict TypeScript,
new-version generation/hash checks and unchanged-role validation passed. No
automated tests were authored or run. Independent Astra review found catalog
entrypoint drift and absolute provenance paths; both were corrected. Global
instructions and orchestration policies retain the preceding release rules,
with explicit no-test and observed-delivery additions. Runtime provenance is
portable and separate from unchanged advisory provenance.

The actual wrapper app-server handshake succeeded using Codex 0.156.1 and the
existing CorrOrtiz account (private fingerprint only in receipt), with 9 percent
primary usage and Sol/Astra/Luna6 in its native model catalog. This made no
inference request and proves neither task transport coverage nor product login.
The current NutriPlan localhost3001 server returns HTTP500; the new task must
repair/verify its environment before accepting screens. Credential locators and
the complete execution packet are private in nutriplan-task.md.

Computer Use recovered through the existing native node_repl integration and
documented @oai/cua plus @oai/sky APIs. Native T3 UI created a dedicated provider
instance codex_nutriplan_headroom, sharing the existing CorrOrtiz shadow home,
and new NutriPlan thread e54c9275-f623-4bd9-9da9-5e79fa2fe4c7 with Sol6High,
standard tier, full access and the current canonical checkout. The actual first
turn responded and read project guidance; a fresh screenshot confirms it.

The first task launch exposed a Codex app-server argument-order defect: T3 adds
MCP -c arguments after app-server, causing the earlier global provider/CUA -c
arguments to be dropped. Headroom had zero requests. The task was interrupted
through normal UI before reconfiguration; no saving was claimed. New immutable
patch1.30.1 moves helper overrides after app-server while preserving T3 argument
order and ordinary CLI behavior. Source syntax/typecheck passed. A real
T3-shaped app-server config/read now confirms headroom_local, its current
loopback URL, and the preserved T3 MCP entry. This still is not inference proof.

Completed: independent1.30.1 delta review passed; package/source31cb814,
SHA256c2ea7fa31d8c63ff16b6509c0b17d4a3d64f6e577e5d3cdaa95e0c7d61bdd777.
The new development prerelease is published at GitHub tagv1.30.1 and installed
through its exact extracted package. Isolated upgrade/audit and operator
upgrade/audit passed. All18 role TOMLs and operator account/config bytes still
match the baseline. No automated tests were run or changed.

The stopped task's exclusively owned wrapper53502 and its Codex/proxy children
were terminated normally; all exited, leaving other T3 conversations intact.
The SAME T3 thread was resumed through native UI at08:46UTC using a fresh
app-server. Headroom invocation headroom-iI37VT, port55451, recorded4 completed
requests for gpt-6-sol and0 failed requests at08:47UTC. This confirms initial
coordinator transport, not complete future descendant coverage or causal savings.
Native first-phase role observations were Sol6High root, two Luna6High mappers,
Astra6XHigh architecture planner and Astra6XHigh QA planner. Service tier was
requested standard; the native records did not expose an observed tier.

Task title: NutriPlan integral · Resumen con visor · Sol High. Its resumed
coordinator acknowledged continuity, the transport correction and the pending
dependency repair. It owns subsequent product changes and DS dependency/lock
adoption. No other product writer is assigned by this session. No result polling
or background supervisor remains; do not stop its active Headroom process.

Evidence: private native-first-turn.json, t3-launch-receipt.json,
headroom-resume-observation.json, operator-preservation-1301.json and
t3-resumed-clean.png. Sanitized HTML report is served at
https://double-emissions-nut-vampire.trycloudflare.com/2a31c0cd7c20592af99b11cf2ed48441/public/development-1301/index.html
until2026-09-24T14:45:32Z. HTTP200 and exact local bytes were verified; the
embedded native screenshot loaded. Product acceptance/costs remain the new
task's responsibility; this session completed release and observed launch.
