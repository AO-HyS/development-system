# Development System Contract 1.4.1

Version 1.4.1 retains every 1.4.0 lifecycle, authorization, adapter, benchmark, repository-preparation, delivery, measurement, privacy, installation, rollback, paid-tool, provider-readiness, Working Backwards gate, Reader, publication, and T3 handoff guarantee. Published 1.4.0 bytes remain unchanged.

## Repository routing consistency amendment

- Make the generated repository JSON and its Codex/Factory Markdown adapters describe the same routing behavior.
- Route an ordinary future customer story, Amazon Working Backwards request, PRFAQ request, or progressive planning request to `working-backwards` without requiring the operator to remember a skill name.
- Keep every other special stage explicit unless its matching flow is directly requested.
- List `working-backwards` in the generated explicit-command contract for Codex/T3 Code and Factory.
- Preserve all human gates and authorization boundaries; intent-aware routing selects a workflow but grants no implementation, publication, merge, release, or production authority.
