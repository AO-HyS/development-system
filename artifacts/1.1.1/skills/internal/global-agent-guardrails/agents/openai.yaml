interface:
  display_name: "Global Agent Guardrails"
  short_description: "Executable destructive-command protection"
  default_prompt: "Use $global-agent-guardrails to install or audit the versioned destructive-command hooks without overwriting existing settings."
